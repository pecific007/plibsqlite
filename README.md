# PLIBSQLITE

- Plibsqlite is a wrapper of python's sqlite3 library.
- This can perform basic C.U.R.D with some convenient functions.
- It is possible to make some advance statements with `WHERE` conditions.
- The SQLite statements are prepared using SQLite's escaping method (by using `?` instead of raw values).
